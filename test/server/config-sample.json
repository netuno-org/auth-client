{
    "api": {
        "services": {
            "prefix": "http://localhost:9000/services/"
        },
        "auth": {
            "storage": "auth-token.json",
            "login": {
                "username": "dev",
                "password": "dev"
            }
        }
    }
}